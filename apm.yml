name: agent-nexus
version: 1.0.0
compilation:
  target:
    - cursor
    - claude
    - antigravity
dependencies:
  apm:
  - fantasy-cc/context-harness
  - obra/superpowers
  mcp:
  - name: sequential-thinking
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@modelcontextprotocol/server-sequential-thinking'
  - name: playwright
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@playwright/mcp@latest'

  - name: context7
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@upstash/context7-mcp@latest'

  - name: nitan-mcp
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@nitansde/mcp@latest'

  # Optional MCPs — included only with `./deploy.sh --all` or
  # when interactively confirmed during deploy.
  optional_mcp:
  - name: github-mcp
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@modelcontextprotocol/server-github'

  - name: notion-mcp
    registry: false
    transport: stdio
    command: npx
    args:
    - -y
    - '@notionhq/notion-mcp-server'
    env:
